{
  "name": "market_sentiment",
  "description": "Real-time market sentiment scan across crypto Twitter, on-chain data, and news. Fear & greed context for Indonesian Web3 traders. Quick pulse check before any trade.",
  "jobFee": 0.1,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "description": "Market scope: 'overall', 'defi', 'gamefi', 'solana', 'base', 'monad' or specific token"
      },
      "language": { "type": "string", "enum": ["english", "indonesian", "mixed"] }
    },
    "required": ["scope"]
  }
}
